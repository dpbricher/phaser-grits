// Generated by CoffeeScript 1.8.0
var Main;

Main = function() {
  var game, tileLayer, tileMap;
  tileMap = null;
  tileLayer = null;
  return game = new Phaser.Game(800, 600, Phaser.AUTO, "content-main", {
    preload: function() {
      game.load.tilemap("map", "data/map1.json", null, Phaser.Tilemap.TILED_JSON);
      return game.load.image("map_tiles", "images/grits_master.png");
    },
    create: function() {
      tileMap = game.add.tilemap("map");
      tileMap.addTilesetImage("grits_master", "map_tiles");
      tileLayer = tileMap.createLayer("floor");
      return tileLayer.resizeWorld();
    },
    update: function() {
      return game.camera.setPosition(game.camera.x + 5, game.camera.y + 5);
    }
  });
};
